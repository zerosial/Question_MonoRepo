{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "dotEnv": [".env"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "dotEnv": [".env"]
    },
    "start": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dotEnv": [".env"]
    }
  },
  "globalDotEnv": [".env"]
}
